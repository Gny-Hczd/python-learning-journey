a=int(input()) 
a1=a
b=[]
say=0
while a>=2:
	s=2
	for i in range(2,a): 
		if a%i!=0:
			s+=1 
	if s==a:
		b.append(a)
	a-=1
prime=[]
for j in range(0,len(b)):
	if a1%b[j]==0:
		prime.append(b[j])
print(f"the largest prime factor of the number {a1} is:{max(prime)}") 


#This is my first attempt at solving #Project Euler Problem 3.  
#The current code uses a brute-force #approach and works fine for small #inputs, but it is very slow for #large numbers.  

#I plan to add a more efficient #version in the coming days 🚀  

