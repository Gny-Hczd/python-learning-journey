a=100
b=100
palindrom=[]
while a<1000 and b<1000:
	a=a+1
	b=b+1
	c=a*b
	if str(c)==str(c)[::-1]:
		palindrom.append(c)
		print(a,",",b,",",c)
		
Actually this isn't the full answer to Project Euler Problem 4, because in my program a and b are always the same, so it only checks numbers where a=b